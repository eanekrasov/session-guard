/**
 * Подделка хостовой полезной нагрузки для теста.
 *
 * Типы событий и сообщений принадлежат `@opencode-ai/plugin` и описывают их
 * целиком. Тест кормит подмножество — ровно те поля, которые читает код под
 * проверкой, — и достраивать остальное значило бы описывать чужой контракт
 * заново, а вслед за ним и чинить его при каждом обновлении SDK.
 *
 * Приведение намеренно узкое и именованное: оно стоит только на границе с
 * хостом. Наши собственные типы через него не проходят — их неполнота обязана
 * оставаться ошибкой компиляции.
 */
export function hostPayload<T>(partial: unknown): T {
  return partial as T;
}
