stages:
  execution:
    loop: implementation
    stages:
      code:
        allowedAgents: ['code']
    transitions:
      - from: code
        to: done
        effects:
          - approve: release
stageAssignments:
  - id: execution
    priority: 1
    condition: 'true'
    result: execution
