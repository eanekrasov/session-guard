extends: 'verify-default/cycle.yaml'
# Finishing the task needs the operator's consent, on the edge that ends it.
stages:
  execution:
    transitions:
      - from: code
        to: done
        consent: release
