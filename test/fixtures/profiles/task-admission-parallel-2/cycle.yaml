stages:
  execution:
    loop: implementation
    dispatch:
      strategy: parallel
      maxConcurrent: 2
    stages:
      dev:
        allowedAgents: ['code']
      review:
        allowedAgents: ['review']
      qa:
        allowedAgents: ['qa']
stageAssignments:
  - id: execution
    priority: 1
    condition: 'true'
    result: execution
