extends: 'cycle-minimal/cycle.yaml'
# A roster naming agents the profile does not ship, for the name-resolution tests.
stages:
  execution:
    stages:
      dev:
        allowedAgents: ['code', 'my-agent', 'fallback-agent']
