extends: 'verify-default/cycle.yaml'
# The approval is granted by an earlier edge and spent by a later one.
stages:
  execution:
    transitions:
      - from: code
        to: verify
        effects:
          - approve: release
      - from: verify
        to: done
        consent: release
