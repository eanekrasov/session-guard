extends: 'cycle-code/cycle.yaml'
# The task finishes only once its own invariants verdict is green.
stages:
  execution:
    stages:
      code:
        allowedAgents: ['coder']
    transitions:
      - from: code
        to: done
        guard: "task.gates.invariants == 'passed'"
