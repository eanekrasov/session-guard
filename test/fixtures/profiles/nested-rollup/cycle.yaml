stages:
  execution:
    loop: implementation
    dispatch:
      strategy: parallel
      maxConcurrent: 5
    stages:
      dev:
        allowedAgents: ['coder']
stageAssignments:
  - id: execution
    priority: 1
    condition: 'true'
    result: execution
