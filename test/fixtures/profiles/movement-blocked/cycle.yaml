extends: 'cycle-code/cycle.yaml'
# Departure waits on a session gate the task cannot close itself.
stages:
  execution:
    retryBudget:
      maximum: 3
    transitions:
      - from: code
        to: done
        guard: "session.gates.review == 'passed'"
