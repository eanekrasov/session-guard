stages:
  execution:
    loop: implementation
    dispatch:
      strategy: serial
    stages:
      dev:
        allowedAgents: ['code', 'reviewer']
stageAssignments:
  - id: execution
    priority: 1
    condition: 'true'
    result: execution
